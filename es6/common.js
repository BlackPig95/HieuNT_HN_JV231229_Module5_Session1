export const formatName = (name) =>
{
    return `Your name is ` + name;
};
const formatDate = () =>
{

};
//export default
const formatMoney = (money) =>
{

};
export default formatMoney;
//Đối với export thường, khi import phải đặt trong {}
//Với export default, không cần bọc trong {}
//Trường hợp 1 file nhiều biến, hàm thì dùng export thường (named export)
//Trường hợp 1 file chỉ có 1 biến hoặc hàm thì dùng export default